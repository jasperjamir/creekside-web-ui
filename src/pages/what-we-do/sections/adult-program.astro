---
import ImageTextSection from '../../../components/sections/ImageTextSection.astro';

interface Props {
  title?: string;
  subheading?: string;
  overviewParagraph?: string;
  approachParagraph?: string;
  image?: { asset?: { url: string }; alt?: string };
}

const {
  title = '',
  subheading = '',
  overviewParagraph = '',
  approachParagraph = '',
  image,
} = Astro.props;
const imageUrl = image?.asset?.url ?? '';
const defaultAlt = 'Creekside Adult Program';
---

<ImageTextSection
  sectionId="adult-program"
  title={title}
  subheading={subheading}
  body={overviewParagraph}
  imageUrl={imageUrl}
  imageAlt={defaultAlt}
  imageLeft={false}
  backgroundColor="white"
/>

<ImageTextSection
  sectionId="adult-program-approach"
  body={approachParagraph}
  imageUrl={imageUrl}
  imageAlt={defaultAlt}
  imageLeft={true}
  backgroundColor="cream"
/>
